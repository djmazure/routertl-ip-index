name: wbscope
namespace: zipcpu
version: "1.0.0"
description: "Wishbone oscilloscope — logic analyzer / trigger core for FPGA debug"
license: LGPL-3.0-or-later

source:
  type: git
  url: https://github.com/ZipCPU/wbscope.git
  # No semver tags on this repo — pinned to a known-good HEAD commit for reproducibility.
  # Update by running: git ls-remote https://github.com/ZipCPU/wbscope.git HEAD
  commit: 999f35f445817f69be3cd826cc66287f4cc760f7
  ip_yml_path: "."

routertl_compat:
  library: zipcpu
  language: verilog
  files:
    - rtl/wbscope.v
    - rtl/wbscopc.v
