name: zipcpu_uart
namespace: zipcpu
version: "1.0.0"
description: "ZipCPU UART — configurable baud, Wishbone and AXI-Lite interface options"
license: LGPL-3.0-or-later

source:
  type: git
  url: https://github.com/ZipCPU/wbuart32.git
  # No semver tags on this repo — pinned to a known-good HEAD commit for reproducibility.
  # Update by running: git ls-remote https://github.com/ZipCPU/wbuart32.git HEAD
  commit: f43a6b83c3a70fb2ac0696a45c5545b233fb860b
  ip_yml_path: "."

routertl_compat:
  library: zipcpu
  language: verilog
  files:
    - rtl/rxuart.v
    - rtl/txuart.v
    - rtl/ufifo.v
    - rtl/wbuart.v
