name: zipcpu_uart
namespace: zipcpu
version: "1.0.0"
description: "ZipCPU UART — configurable baud, Wishbone and AXI-Lite interface options"
license: LGPL-3.0-or-later

source:
  type: git
  url: https://github.com/ZipCPU/wbuart32.git
  tag: master    # no semver tags on this repo — pinned via lock file commit SHA
  ip_yml_path: "."

routertl_compat:
  library: zipcpu
  files:
    - rtl/rxuart.v
    - rtl/txuart.v
    - rtl/ufifo.v
    - rtl/wbuart.v
