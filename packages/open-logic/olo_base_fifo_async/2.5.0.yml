name: olo_base_fifo_async
namespace: open-logic
version: "2.5.0"
description: "Asynchronous FIFO — configurable depth/width, CDC-safe two-clock design"
license: LGPL-2.1-or-later

source:
  type: git
  url: https://github.com/open-logic/open-logic.git
  tag: v2.5.0
  ip_yml_path: "."   # path inside repo where ip.yml lives (if present)

# Compatibility shim — used when the IP repo has no ip.yml.
# List only the files required for this specific IP block.
routertl_compat:
  library: olo
  files:
    - src/base/vhdl/olo_base_pkg.vhd
    - src/base/vhdl/olo_base_fifo_async.vhd
